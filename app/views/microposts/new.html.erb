
<h1>New Post</h1>
<%= render 'form' %>
<%= link_to 'Back', microposts_path %>

<% @microposts.each do |micropost| %>
  <tr>
    <td><%= micropost.content %></td>
    <td><%= micropost.name %></td>
<% end %>
